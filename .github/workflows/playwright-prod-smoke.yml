name: Smoke Tests - PROD Environment

# Disabled while the project is TEST-only.
# Keep this workflow inert until TEST and UAT are approved and PROD is ready.
on: []

jobs:
  smoke-prod:
    name: Smoke Tests PROD
    if: ${{ false }}
    runs-on: ubuntu-latest

    steps:
      - name: PROD disabled
        run: echo "PROD workflow disabled. Run only TEST while the project is not ready for UAT/PROD."
