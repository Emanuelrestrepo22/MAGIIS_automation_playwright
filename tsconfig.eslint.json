// tsconfig.eslint.json
{
  "extends": "./tsconfig.json",
  "include": [
    ".eslintrc.json",         // ✅ ESLint config
    "**/*.ts"                 // ✅ Todos los archivos TypeScript
  ],
  "exclude": [
    "node_modules",           // ⛔ Evita analizar dependencias
    "playwright-report",      // ⛔ Reportes de Playwright (si existen)
    "dist",                   // ⛔ Código transpilado (opcional)
    "coverage"                // ⛔ Carpeta de cobertura (si se usa)
  ]
}
